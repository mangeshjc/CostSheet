__pycache__/
*.pyc
media/
.venv/
venv/

# Build artifact: regenerated by `collectstatic` on every deploy.
staticfiles/

# Speed cache only; rebuilds itself on first use.
sheet_tabs_cache.json

# NOTE: google_token.json and db.sqlite3 are committed on purpose, so the repo
# carries everything needed to run. google_token.json holds a live Google
# refresh token -- this repo MUST stay private, and if it is ever made public
# the token and client secret have to be rotated immediately.
